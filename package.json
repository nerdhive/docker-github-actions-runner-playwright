{
  "name": "github-actions-runner-image",
  "private": true,
  "description": "Build context for a self-hosted GitHub Actions runner image with Playwright Chromium preinstalled.",
  "packageManager": "pnpm@11.25.0",
  "engines": {
    "node": ">=24 <25"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
